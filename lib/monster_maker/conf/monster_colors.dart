import 'package:flutter/material.dart';

class MonsterColors {
  static const List<Color> bodyColors = [
    Color(0xFF66B7AC), // Teal (from your existing set)
    Color(0xFFFD7448), // Vibrant Orange
    Color(0xFF3DA7F3), // Sky Blue
    Color(0xFFFC5DA8), // Cheerful Pink
    Color(0xFF4DDC5B), // Fresh Green
    Color(0xFFFFCD3C), // Bright Warm Yellow
  ];

  static const List<Color> hornColors = [
    Color(0xFFE6D3B3), // Light Beige / Bone
    Color(0xFFBFA48A), // Warm Beige
    Color(0xFF8B5E3C), // Medium Brown
    Color(0xFF5C4033), // Dark Brown
    Color(0xFF888888), // Neutral Gray
  ];
}
